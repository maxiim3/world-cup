import {CardLayout} from "../Layouts/CardLayout"
import React from "react"

export function SemiFinalComponent({updateStatus}: {updateStatus: Function}) {
	return (
		<CardLayout title={"Semi Final"}>
			{/*<MatchTableTemplate teams={teams} />*/}
			{/*<MatchTableTemplate teams={teams} />*/}
		</CardLayout>
	)
}